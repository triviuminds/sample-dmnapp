organization=triviuminds
dmnappVersion=1.0.0

# optional, by default, the version of the dmn spec is 1.5
dmnSpecVersion=1.5 
# optional, by default, the api spec file name is 'openapi.json'
apiSpecName=openapi.json

org.gradle.jvmargs=-Xmx2048m

nexusUser=
nexusPassword=
nexusUrl=
